export const content = [
  {
    paddingTop: 10,
    paddingBottom: 10,
    paddingLeft: 20,
    paddingRight: 20,
    fontSize: 18
  },
  ({ theme: { color, font } }) => ({
    color: color.text.primary,
    fontFamily: font.family.primary
  })
]

export default {
  content
}
