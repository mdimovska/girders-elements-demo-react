export const title = [
  {
    fontSize: 30,
    fontWeight: 'bold'
  },
  ({ theme: { color, font } }) => ({
    color: color.text.inverted,
    fontFamily: font.family.primary
  })
]

export default {
  title
}
